@page
@model HistoricoModel
@{
    ViewData["Title"] = "Histórico";
    bool contemDisciplinas = Model.DisciplinasPorModulo!.Count > 0;
}

<link rel="stylesheet" href="~/css/login/login.css">
<div class="text-center dimention">
    <div class="dft-header-title">
        <h1>@ViewData["Title"]</h1>
    </div>
    <div id="container-table">
        <table id="tblResultado">
            <tbody>
                @if (contemDisciplinas)
                {
                    @foreach (var modulo in Model.DisciplinasPorModulo)
                    {
                        <tr>
                            <td><span>Módulo </span>modulo.Id</td>
                            <td>modulo.Nome</td>
                        </tr>
                        <tr>
                            <td>Disciplina</td>
                            <td>Requisitos</td>
                            <td>Situação</td>
                            <td>Carga Horária</td>
                            <td>Nota</td>
                        </tr>
                        @foreach (var disciplina in modulo.Value)
                        {
                            <tr>
                                <td>disciplina.Nome</td>
                                <td>disciplina.Requisitos</td>
                                <td>disciplina.Situacao</td>
                                <td>disciplina.CargaHoraria</td>
                                <td>disciplina.Nota</td>
                            </tr>
                            @* @if (disciplina.IdModulo == modulo.Id)
                {
                } *@
                        }
                    }
                }
                else
                {
                    <tr>
                        <td>Ops! Histórico indisponível no momento.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
